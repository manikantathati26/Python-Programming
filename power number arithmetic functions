def my_function(base,exponent):
    power=base**exponent
    return power
base=int(input("enter the base value:"))
exponent=int(input("enter the exponenet value:"))
print("power number =",my_function(base,exponent))
